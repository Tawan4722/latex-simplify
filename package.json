{
  "name": "latex-simplify",
  "version": "1.0.0",
  "description": "Scientific math editor with live LaTeX rendering and copy functions",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server -p 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
